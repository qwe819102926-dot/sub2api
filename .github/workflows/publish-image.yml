name: Build and publish Docker image

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: publish-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Build once, test the loaded image, then push that exact image. This avoids
  # rebuilding the same Dockerfile in a second runner after the smoke gate.
  build-smoke-publish:
    # Build on the persistent Docker host so Go, pnpm and BuildKit caches
    # survive between workflow runs. This runner is reserved for trusted main
    # branch builds; untrusted pull requests stay on GitHub-hosted runners.
    runs-on: [self-hosted, linux, x64, sub2api-builder]

    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          name: sub2api-builder
          cleanup: false

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set image metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/qwe819102926-dot/sub2api
          tags: |
            type=raw,value=latest
            type=sha,prefix=sha-

      - name: Build image once (no push)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64
          push: false
          load: true
          tags: |
            sub2api:smoke
            ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            NPM_CONFIG_REGISTRY=https://registry.npmmirror.com
          cache-from: type=registry,ref=ghcr.io/qwe819102926-dot/sub2api:buildcache
          cache-to: type=registry,ref=ghcr.io/qwe819102926-dot/sub2api:buildcache,mode=max

      - name: Run smoke tests
        run: sh deploy/smoke-test.sh sub2api:smoke 8080

      - name: Push tested image
        run: docker push --all-tags ghcr.io/qwe819102926-dot/sub2api
