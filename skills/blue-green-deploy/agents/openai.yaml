interface:
  display_name: "蓝绿部署"
  short_description: "Docker 应用零停机蓝绿发布、健康检查与自动回滚"
  default_prompt: "使用 $blue-green-deploy 执行本项目的 Docker 蓝绿部署；先从当前目录或其直接子目录定位包含 deploy/blue-green-update.sh、backend/ 和 frontend/ 的 Git 仓库根目录。"

policy:
  allow_implicit_invocation: true
